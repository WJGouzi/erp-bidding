# 标书生成规则（Agent 行为规范）

## 一、数据溯源与中间过程

1. 生成标书的中间过程数据是后续生成标书所依赖的内容，必须严格遵守上传的招标文件和招标附件信息。
2. 生成的目录、解析内容、确认事项等必须严格遵照原始招标文件解析出来的内容，**不能使用同一个模板**。
3. 目录是生成标书的大纲，解析内容是梗概，具体填充内容需参考知识库、产品库以及原始招标文件及附件。
4. 严禁使用 LLM 进行推导，所有数据都要有来源。如果在生成标书时找不到对应内容，**直接留空**，不得自行编造。

## 二、资格性文件

### 2.1 模板复制
- 招标书原文或附件中有模板要求（含文字模板和表格模板）的，必须**全文原样复制**到生成的标书中，**禁止 LLM 自由发挥填充内容**
- 如果招标书原文没有对应的模板，才可以填充内容
- 标题也必须按照招标文件原文的要求填写，严禁 LLM 随意生成
- 若模板同时包含表格和文字段落，**不得丢弃任何内容**，必须完整保留

### 2.2 格式与排版
- 如果招标书或附件有要求内容格式（含字体、文字大小等），必须直接复制到生成的标书中，**严禁修改格式**
- 每个资格性内容**必须独占一页**，切勿多个资格性内容在同一页

### 2.3 数据来源
- 涉及主体信息的内容直接从主体信息表获取
- 其余内容可以从知识库获取

### 2.4 章节顺序
严格按招标文件中的章节顺序生成资格性文件。例如第一章是"法人证明材料"，则在生成的标书中第一章必须是法人证明材料，不得出现"公司资质证明"等其他内容。

## 三、表格识别与处理

1. 表格必须用**专门的工具识别和分析**，禁止将表格当作文本段落处理
2. 实际招标文件中的表格可能包含**合并单元格、跨行跨列、嵌套表格、不规则表头**等复杂结构，上述结构必须正确识别和还原。如现有解析器无法完整还原，至少要做到：
   - 保留原始表格的**完整行和列数据**，不得丢失任何单元格内容
   - 保留文字和表格的混合排版顺序，不得错乱
   - 识别并保留表头层级关系
3. 生成标书时表格内容严格复制原文，**只填写原文中明确空缺的部分**
4. 产品信息等表格必须严格复制原文格式
5. 当表格和文字段落同时存在时，**不得丢弃任何内容**
6. 当前整个标书生成流程中表格识别全部错误，此问题必须优先解决

## 四、商务要求

商务要求是投标书生成时的约束性条件（不遵守则废标）。生成内容时必须严格遵循商务要求，不得遗漏或随意修改。

## 五、合同模板

招标文件中包含的合同模板/合同条款信息，不属于投标书内容。生成标书时必须排除合同相关章节。

## 六、封面样式

若"响应文件格式"中有封面样式声明（包括格式、布局、内容项），必须使用给定的封面格式，**禁止自行编写**。如果能找到填充的内容，则需要填充上，找不到就空着。

## 七、章节分页与标书排版

### 7.1 分页
各章节生成后必须换页，禁止两个章节的内容出现在同一页。每个章节独立起始于新的一页。

### 7.2 字体格式
生成标书时对于字体要严格遵守如下要求
- 正文：仿宋，小四
- 一级标题：宋体，三号
- 二级标题：宋体，小三
- 三级标题：宋体，四号
- 表格中的文字：仿宋，小四

## 八、专家思维与质量检查

1. 生成内容或修改内容时，不能只着眼于上传的一份资料，需以专家思维处理
2. 参考文件夹：`/Users/wangjun/Desktop/招标文件` 中的全部招标文件
3. 生成的内容必须经过合理性检查，不能有明显错误存在

## 九、章节层级与归属识别

解析招标文件时，必须正确识别章节的父子/从属关系。例如大标题为"响应文件格式"时，其下所有子章节均属格式要求范畴，不得将子章节内容错误归类到其他章节。分片策略需保留章节层级结构，确保后续生成时能正确还原原文组织。

## 十、通用原则

- **数据溯源**：生成标书的所有中间数据必须严格基于原始招标文件和附件，不得使用 LLM 推断
- **禁止 LLM 自由发挥**：所有内容必须有来源，找不到对应内容时直接留空
- **模板优先**：原文有模板则必须原样复制，无模板才可填充
- **合理性检查**：生成的内容必须经过检查，不能有明显的错误
