## 1. 修改"正本"浮动表格定位参数

- [x] 1.1 计算并替换第一处（L~3940，pre-loop 前）的 tblpX 值为 6073775 EMU（对应右边缘距页面右边 640 twip）
- [x] 1.2 计算并替换第二处（L~4110，main-loop 第一封面）的 tblpX 值为 6073775 EMU
- [x] 1.3 计算并替换第三处（L~4520，后续封面）的 tblpX 值为 6073775 EMU
- [x] 1.4 验证三处注释与实际值一致（注释更新为"右边缘距页面右边 640 twip"）

## 2. 实现垂直居中

- [x] 2.1 在第一处（pre-loop 前）单元格文本设置后添加 `w:vAlign="center"` XML 元素
- [x] 2.2 在第二处（main-loop 第一封面）添加相同的垂直居中代码
- [x] 2.3 在第三处（后续封面）添加相同的垂直居中代码

## 3. 验证

- [x] 3.1 确认三处 tblpX、tblpY 值正确（tblpY=406400 EMU 保持不变）
- [x] 3.2 确认三处均添加了垂直居中设置
- [x] 3.3 生成测试封面并验证"正本"定位和居中效果
